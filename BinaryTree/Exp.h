#ifndef EXP_H
#define EXP_H
using namespace std;

class node{
    public:
        int data;
        /* node* left;
        node* right;
        node(int d);
        node* buildTree(node* root); */
        node();
        void fuckme();
};

#endif